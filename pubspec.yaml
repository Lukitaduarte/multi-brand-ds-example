name: multi_brand_ds_example
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=3.2.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  equatable: ^2.0.3
  widgetbook_annotation: ^3.1.0
  widgetbook: ^3.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner:
  widgetbook_generator: ^3.7.0
  flutter_lints: 2.0.0

flutter:
  uses-material-design: true